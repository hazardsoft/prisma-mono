name: 'Prepare'
description: 'Sets up the repository'
runs:
  using: 'composite'
  steps:
    - name: Install PNPM
      uses: pnpm/action-setup@v2
      with:
        version: latest
    
    - name: Install Dependencies
      shell: bash
      run: pnpm install
