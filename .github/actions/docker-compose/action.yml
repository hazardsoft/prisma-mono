name: 'Docker-compose'
description: 'Installs docker-compose command'
runs:
  using: 'composite'
  steps:
    - name: Create folder for Docker plugins
      shell: bash
      run: mkdir -p /usr/local/lib/docker/cli-plugins
    - name: Download Docker-Compose plugin
      shell: bash
      run: curl -SL https://github.com/docker/compose/releases/download/v2.21.0/docker-compose-linux-x86_64 -o /usr/local/lib/docker/cli-plugins/docker-compose
    - name: Make plugin executable
      shell: bash
      run: chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
